'use strict';

// let i = 0;
// while (i != 10) {
//   i += 0.2;
// }

// Цикл не завершится, потому что 0.2 - это не точное значение и прибавление его приведет к тому, что ровно 10 никогда не получится
